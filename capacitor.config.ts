import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'BANKSapi-coding-challange',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
